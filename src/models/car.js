function Car(marca, modelo,color, anio, km, precio, cuotas, patente, vendido) {
    this.marca = marca
    this.modelo = modelo
    this.color = color
    this.anio = anio
    this.km = km
    this.precio = precio
    this.cuotas = cuotas    
    this.patente = patente  
    this.vendido = vendido
}
module.exports = Car